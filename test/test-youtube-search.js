var chai = require('chai');
//var yt = require('youtube-search.js')

describe('youtube-search.js', () => {
    describe('#search', () => {
        it('should return given number of results');
        it('should contain video id');
        it('should contain video title');
        it('should contain video thumbnail url');
    });
});