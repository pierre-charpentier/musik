var chai = require('chai');
var assert = chai.assert;

describe('youtube-download.js', () => {
    describe('#download', () => {
        it('should create a mp3 file named with the video id');
        it('should create a non-empty file');
    });
});