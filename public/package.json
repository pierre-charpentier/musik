{
  "name": "musik-client",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-scss": "node-sass --include-path scss scss -o css",
    "watch-scss": "nodemon -e scss -x \"npm run build-scss\""
  },
  "author": "Qayou",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.1.1",
    "nodemon": "^1.11.0"
  }
}
